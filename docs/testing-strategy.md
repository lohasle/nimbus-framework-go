# 测试策略

测试按风险分层：

1. 单元测试：Token 类型、解析、分页和金额计算。
2. 路由契约：当前开放菜单依赖的 HTTP 方法与路径必须存在。
3. MySQL 集成：空库初始化、重复启动、CRUD、租户隔离和事务回滚。
4. 前端静态检查：TypeScript、Lint、生产构建。
5. 浏览器验收：登录、刷新、退出及当前菜单的查询和保存操作。

路由存在只能证明没有 404，不能替代数据库和浏览器功能验收。
